pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        flatDir { dirs 'xposed/libs' }
    }
}
rootProject.name = "HMSPush"
include ':app'
include ':xposed'
include ':common'

include ':interface'
project(":interface").projectDir = file("libxposed-service/interface")

include ':service'
project(":service").projectDir = file("libxposed-service/service")